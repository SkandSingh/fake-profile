# Essential packages for Text Analysis API
# These should install without issues on Python 3.13

# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0
python-multipart==0.0.10

# Basic data processing
numpy>=1.24.0
requests>=2.31.0

# ML libraries (install without strict version requirements)
torch>=2.0.0
transformers>=4.30.0

# Optional: if transformers installation fails, we can run in mock mode
# The text analysis service has fallback functionality

# Development tools
pytest>=8.0.0
httpx>=0.27.0