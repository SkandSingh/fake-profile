# Offline-only: used to run backend/ml/train_metrics_model.py, which
# produces frontend/lib/metricsModel.json. Nothing in this directory is
# deployed - the deployed app is frontend/ (Vercel), which needs none of
# these packages.
pandas>=2.2.0
scikit-learn>=1.5.0
numpy>=1.26.0
