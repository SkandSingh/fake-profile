# Everything runs inside this Next.js app's own API routes (see
# app/api/analyze, app/api/extract) - there is no separate backend to point
# at anymore. Set the real value in the Vercel project's Environment
# Variables settings, not in this file (this file is committed to git).
GEMINI_API_KEY=your-gemini-api-key-here
